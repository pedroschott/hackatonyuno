# AgentPay

> AgentPay is an authorization and enforcement layer for agent purchases. It lets a user create a narrow payment mandate, approve it with a passkey, and revoke it immediately.

## Public entry points

- [Landing page](https://agentpay-yuno.vercel.app/): what AgentPay does, for buyers and for merchants.
- [Privacy Policy](https://agentpay-yuno.vercel.app/privacy): how AgentPay handles personal information.
- [Terms of Service](https://agentpay-yuno.vercel.app/terms): the terms governing use of AgentPay.
- [Connect an MCP client](https://agentpay-yuno.vercel.app/connect): user-facing instructions for connecting an authenticated agent.
- [Merchant demo](https://agentpay-yuno.vercel.app/store): an example merchant with one canonical page per product at `/store/products/:id`.
- [Merchant demo catalog](https://agentpay-yuno.vercel.app/api/store/catalog): the store-owned catalog endpoint an agent queries through `find_products`.
- [MCP endpoint](https://agentpay-yuno.vercel.app/mcp): OAuth-protected Streamable HTTP MCP server.
- [MCP protected-resource metadata](https://agentpay-yuno.vercel.app/.well-known/oauth-protected-resource): OAuth discovery for the MCP endpoint.
- [Merchant discovery example](https://agentpay-yuno.vercel.app/.well-known/agentpay.json): an example of a merchant-owned AgentPay manifest, including its catalog endpoint, categories and currency.
- [Agent guide](https://agentpay-yuno.vercel.app/docs/agents): the MCP tool order, what each result means and how to act on every decision.
- [Merchant documentation](https://agentpay-yuno.vercel.app/docs): how a store installs and sets up the AgentPay merchant SDK.
- [Merchant console](https://agentpay-yuno.vercel.app/developers): register a merchant ID, create a hosted test store, manage products and keys, and inspect checkout decisions.
- [Supported live stores](https://agentpay-yuno.vercel.app/api/stores): verified public store and discovery URLs. This list is empty until a real merchant verifies and opts in.

## Identity and fraud boundary

- Account holders complete a hosted Didit v3 identity and fraud-verification workflow before authorizing a mandate or one-time exception.
- AgentPay stores only Didit's minimal session and entity status. Documents, images, biometrics, provider tokens, and the full decision are not copied into AgentPay.
- Didit webhooks are timestamp-bounded, HMAC-verified, de-duplicated, and reconciled against the decision API after the hosted redirect.

## Merchant integration

- [Quickstart](https://agentpay-yuno.vercel.app/docs/quickstart): a new store accepting agent purchases with two routes.
- [Install the SDK](https://agentpay-yuno.vercel.app/docs/installation): requirements and the one-command installer.
- [Publish discovery](https://agentpay-yuno.vercel.app/docs/discovery): serving `/.well-known/agentpay.json` and a catalog endpoint.
- [Protect checkout](https://agentpay-yuno.vercel.app/docs/checkout): verification, decisions and charging safely.
- [Framework recipes](https://agentpay-yuno.vercel.app/docs/frameworks): Next.js, Hono, Express, Fastify and edge runtimes.
- [Test the integration](https://agentpay-yuno.vercel.app/docs/testing): offline signed requests and a live revocation rehearsal.
- [SDK reference](https://agentpay-yuno.vercel.app/docs/reference): exported functions and types.
- [Protocol and registry](https://agentpay-yuno.vercel.app/docs/reference/protocol): signed request format and registry endpoints.
- [Decisions and reason codes](https://agentpay-yuno.vercel.app/docs/reference/decisions): every outcome a merchant can receive.
- [Troubleshooting](https://agentpay-yuno.vercel.app/docs/troubleshooting): the failures merchants actually hit.
- [Merchant console and stores](https://agentpay-yuno.vercel.app/docs/stores): merchant onboarding, hosted test URLs, API keys, and supported live-store policy.

## Protocol documentation

- [Repository README](https://github.com/pedroschott/hackatonyuno#readme)
- [Route and endpoint inventory](https://github.com/pedroschott/hackatonyuno/blob/main/docs/routes.md)
- [Merchant SDK summary](https://github.com/pedroschott/hackatonyuno/blob/main/docs/merchant-sdk.md)
- [Architecture](https://github.com/pedroschott/hackatonyuno/blob/main/docs/architecture.md)
- [Security decisions](https://github.com/pedroschott/hackatonyuno/blob/main/docs/decisions.md)

## Agent operating rules

- Authenticate to the MCP endpoint before using tools. The working order is `get_account`, `find_products`, `create_mandate`, `get_mandate`, `check_purchase`, `purchase`.
- Discover each store from its own URL, which must come from the user or from search. AgentPay's supported-store endpoint contains only verified URLs; it is not a copied catalog or product-ranking directory.
- Use `find_products` with any URL on the store. It returns the exact merchant id, category slugs, currency, prices in cents and product ids. Never guess any of these from a page, a name, a SKU or a URL slug. A store without a catalog endpoint puts the exact product id in `<meta name="agentpay:product_id">` and JSON-LD `productID` on its product page.
- Start with `get_account`. If identity verification is required, direct the user to its `verification_url` and wait for the hosted Didit flow. If payment setup is required, use the browser link returned by `get_payment_setup_link` and never request card numbers, CVCs, PINs, bank passwords, or vault credentials in chat.
- Create the narrowest mandate that matches the user's request with `create_mandate`, passing `merchant_urls` and the exact categories. Send the user `authorization_url` and poll `get_mandate` until it is `active`; a draft is a normal state, never a reason to create a second mandate.
- Express every product price and mandate limit in USD integer cents. AgentPay does not convert currencies; a store that quotes another currency is refused with `CURRENCY_MISMATCH`.
- Call `check_purchase` before `purchase`. Both return `explanation`, `remedy` and `next_tool` on anything but approval. `escalated` means send the user `approval_url`, then call `purchase` again with `exception_id`. `MERCHANT_NOT_IN_SCOPE`, `CATEGORY_NOT_IN_SCOPE`, `CUMULATIVE_EXCEEDED` and `USES_EXCEEDED` are fixed with `amend_mandate`, which the user signs once and which retires the old mandate automatically. Never retry a refused purchase unchanged, and never revoke a mandate to fix its scope.
- Revoke the mandate immediately when the user asks to stop.

## Indexing boundary

The sitemap intentionally contains only public HTML pages. APIs, OAuth flows, the MCP transport, authenticated screens, payment setup and registry endpoints are protocol surfaces rather than crawlable content; their supported methods and trust boundaries are listed in the route inventory.
